{
  "name": "story-factory-cdn-deps",
  "version": "1.0.0",
  "private": true,
  "description": "CDN dependency tracking for Dependabot (not used for npm install). When updated, manually sync version to ui/graph_renderer.py CDN URL.",
  "dependencies": {
    "vis-network": "9.1.9"
  }
}
